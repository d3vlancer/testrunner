name: iOS Remote Debugging Host

on:
  workflow_dispatch:
    inputs:
      region:
        description: 'Tunnel region (ex: us, eu, ap, sa)'
        required: true
        default: 'sa'

jobs:
  configure-host:
    runs-on: macos-latest
    
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4
 
    - name: Setup Xcode 26.3
      uses: maxim-lobanov/setup-xcode@v1 
      with:
        xcode-version: '26.3'
 
    - name: Configure SSH Server
      env:
        RUNNER_PWD: ${{ secrets.MACOS_RUNNER_PASSWORD }}
      run: |
        # Ativa o Login Remoto (SSH) no macOS
        sudo systemsetup -setremotelogin on
         
        sudo sysadminctl -password "$RUNNER_PWD" -user runner
         
        sudo sed -i '' 's/#PasswordAuthentication yes/PasswordAuthentication yes/g' /etc/ssh/sshd_config
         
        sudo launchctl kickstart -k system/com.openssh.sshd
   
    - name: Setup LocalXpose
      env:
        LX_TOKEN: ${{ secrets.LOCALXPOSE_TOKEN }}
      run: | 
        brew install --cask localxpose
         
        open -a Terminal
        sleep 3
        
        # Usa o AppleScript para digitar o comando de login, aguardar e digitar o token simulando o teclado físico
        osascript -e "
          tell application \"System Events\"
            # Digita o comando inicial no terminal aberto
            keystroke \"/opt/homebrew/bin/loclx account login\"
            key code 36 -- Tecla Enter
            delay 3
            
            # Digita o token do secret caractere por caractere
            keystroke \"$LX_TOKEN\"
            key code 36 -- Tecla Enter
            delay 3
          end tell
        "
        echo "Automação de login concluída."
        
    - name: Start LocalXpose Tunnel (SSH Port 22)
      run: |
        /opt/homebrew/bin/loclx tunnel tcp --to :22 --region ${{ github.event.inputs.region }} > lx.log &
        sleep 5
        
        LX_URL=$(grep -oE '[a-z0-9]+\.loclx\.io:[0-9]+' lx.log | head -n 1)
        
        if [ -z "$LX_URL" ]; then
          echo "❌ ERRO: O túnel do LocalXpose não iniciou."
          cat lx.log
          exit 1
        fi
        
        echo "=========================================================="
        echo "CONEXÃO SSH DISPONÍVEL (LOCALXPOSE):"
        echo "Endereço: $LX_URL"
        echo "Usuário: runner"
        echo "=========================================================="
 
    - name: Keep Alive & Listen for Connection
      run: |
        echo "waiting for connections..."
        touch /tmp/keepalive
        while [ -f /tmp/keepalive ]; do
          sleep 30
        done 
        echo "Exiting."
